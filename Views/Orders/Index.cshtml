@model IEnumerable<NBS2019.Models.TestModels.Order>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderPostId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderPost1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderedBy.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderPostId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderPost1.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
